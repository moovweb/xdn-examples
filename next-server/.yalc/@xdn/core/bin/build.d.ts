declare const _default: () => Promise<void>;
/**
 * The default build script for the XDN.  Clears the previous build and
 * builds a .xdn directory with lambda and s3 subdirectories
 */
export = _default;
