import Response from '../router/Response';
import Request from '../router/Request';
/**
 * Serves assets in local development.
 * @private
 * @param req
 * @param res
 */
export default function serveStatic(req: Request, res: Response): void;
