/**
 * Merges the provided config into package.json and writes the result to disk.
 * @param config
 * @return The resulting package.json contents as an object
 */
export default function addToPackageJson(contents: any): any;
